# ================================
# Configuration Backend Docker Monitor
# ================================

# Port du serveur API
PORT=3001

# Environnement (development | production | test)
NODE_ENV=development

# Chemin vers le socket Docker
# En local: /var/run/docker.sock
# Dans un container: monter le volume vers ce chemin
DOCKER_SOCKET_PATH=/var/run/docker.sock

# Timeout pour les opérations Docker (en ms)
DOCKER_TIMEOUT=30000

# Origines autorisées pour CORS
# En dev: http://localhost:3000
# En prod: https://votre-domaine.com
CORS_ORIGIN=http://localhost:3000

# Niveau de log (debug | info | warn | error)
LOG_LEVEL=info
